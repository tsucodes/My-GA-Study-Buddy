// define all DOM elements that I need to target
// create event listener for container-card to flip when clicked
// 



// define Dom elements to manipulate
let card = document.querySelector(".container-cards #flip")
let question = document.querySelector(".question-side")
let answer = document.querySelector(".answer-side")
let correctBtn = document.querySelector(".correct")
let incorrectBtn = document.querySelector(".incorrect")
let nextBtn = document.querySelector(".next")
let resetBtn = document.querySelector(".reset")

// questions stored in an array of objects 

// functions
//  for inital card

// event listeners