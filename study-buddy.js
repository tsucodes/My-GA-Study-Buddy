// define all DOM elements that I need to target
// create event listener for container-card to hide answer but display when clicked
// create an event listener for container-card to flip when clicked
// create an array of objects that hold Q's & A's
// how will the click event listener access index of Q &A?
// 



// define Dom elements to manipulate
let card = document.querySelector(".container-cards #flip")
let question = document.querySelector(".question-card")
let answer = document.querySelector(".answer-card")
let correctBtn = document.querySelector(".correct")
let incorrectBtn = document.querySelector(".incorrect")
let nextBtn = document.querySelector(".next")
let resetBtn = document.querySelector(".reset")

// questions stored in an array of objects 

// functions
//  for inital card

// event listeners